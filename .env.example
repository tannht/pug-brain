# NeuralMemory Environment Variables
# Copy this file to .env and fill in values as needed.

# --- Core ---
# Data directory (default: ~/.neuralmemory)
# NEURALMEMORY_DIR=/path/to/data

# Default brain name
# NMEM_BRAIN=default

# --- Server ---
# Host and port for the API server
# NMEM_HOST=127.0.0.1
# NMEM_PORT=8000

# Trusted networks (CIDR notation, comma-separated)
# NEURAL_MEMORY_TRUSTED_NETWORKS=172.17.0.0/16

# --- Encryption ---
# Enable memory encryption at rest
# NMEM_ENCRYPTION_ENABLED=false
# NMEM_ENCRYPTION_KEYS_DIR=/path/to/keys

# --- Sync (Hub) ---
# Hub server URL for multi-device sync
# NMEM_HUB_URL=https://your-hub.example.com
# NMEM_DEVICE_ID=my-laptop

# --- FalkorDB (optional graph backend) ---
# FALKORDB_HOST=localhost
# FALKORDB_PORT=6379
# FALKORDB_PASSWORD=

# --- Embedding (optional, for cross-language recall) ---
# Required only if using API-based embedding providers.
# Local sentence_transformer provider needs no API key.
# GEMINI_API_KEY=
# OPENAI_API_KEY=

# --- Claude Code hooks ---
# Session ID (auto-set by Claude Code)
# CLAUDE_SESSION_ID=
